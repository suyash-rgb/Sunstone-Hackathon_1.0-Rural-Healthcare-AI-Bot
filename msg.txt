fix(data_ingestion): scrub HTML tags from scheme texts

- Update clean_val function in seed_health_schemes.py
- Add HTML tag removal (br, p, etc.) and entity unescaping
- Preserve formatting like newlines and hyphens for clean embeddings
- Prevent garbage markup from polluting the RAG Vector DB
